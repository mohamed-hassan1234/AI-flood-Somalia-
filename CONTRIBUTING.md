# Contributing

Use task-scoped branches and commits. Routes stay thin, services own use cases,
and repositories own persistence. Every schema change requires Alembic. Never
commit secrets, production data, raw rasters, or sensitive field evidence.

